// todo сторінка з фільмами ( запихнути MoviesList )


import {MoviesList} from "../../Components";

const MoviePage = () => {
    return (
        <div>
            MoviePage
            <MoviesList/>
        </div>
    );
};

export {MoviePage};